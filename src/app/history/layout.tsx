import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "History - Compost Connect",
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}






